id: remove_metadata

enabled: true

min_files: 1
max_files: 1

engine: ffmpeg

menu:
  group: image
  priority: 3

mime:
  - image/*

output:
  extension: png
  preserve_extension: false
  suffix: clean

command:
  args:
    - -map_metadata
    - "-1"
