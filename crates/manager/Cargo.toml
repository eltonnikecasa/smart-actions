[package]
name = "manager"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "0.33"

egui = "0.33"

serde = { version = "1", features = ["derive"] }

serde_yaml = "0.9"

smart_actions_core = { path = "../action_core" }
